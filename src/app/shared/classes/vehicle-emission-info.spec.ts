import { VehicleEmissionInfo } from './vehicle-emission-info';

describe('VehicleEmissionInfo', () => {
  it('should create an instance', () => {
    expect(new VehicleEmissionInfo()).toBeTruthy();
  });
});
